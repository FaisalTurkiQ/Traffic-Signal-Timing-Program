<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intersection Signal Timer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .signal {
            width: 100px;
            height: 100px;
            border: 2px solid #333;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: bold;
        }
        .active {
            background-color: #28a745;
            color: white;
        }
        .inactive {
            background-color: #6c757d;
            color: white;
        }
        .yellow {
            background-color: #ffc107;
            color: black;
        }
        .intersection {
            position: relative;
            width: 300px;
            height: 300px;
            margin: 0 auto;
        }
        .road {
            position: absolute;
            background-color: #333;
        }
        .road-vertical {
            width: 60px;
            height: 100%;
            left: 120px;
        }
        .road-horizontal {
            width: 100%;
            height: 60px;
            top: 120px;
        }
        .signal-1 { position: absolute; top: 0; left: 100px; }
        .signal-2 { position: absolute; top: 100px; right: 0; }
        .signal-3 { position: absolute; bottom: 0; right: 100px; }
        .signal-4 { position: absolute; bottom: 100px; left: 0; }
    </style>
    <script>
        function updateTimer() {
            location.reload();
        }
        setInterval(updateTimer, 1000);
    </script>
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Intersection Signal Timer</h1>
        <div class="intersection mb-4">
            <div class="road road-vertical"></div>
            <div class="road road-horizontal"></div>
            <% signals.forEach((signal, index) => { %>
                <div class="signal signal-<%= index + 1 %> <%= currentSignal === signal.name ? (currentState === 'Green' ? 'active' : 'yellow') : 'inactive' %>">
                    <%= signal.name %>
                </div>
            <% }); %>
        </div>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title text-center">Current Status</h2>
                        <p class="text-center fs-4">Signal: <strong><%= currentSignal %></strong></p>
                        <p class="text-center fs-4">State: <strong class="<%= currentState === 'Green' ? 'text-success' : 'text-warning' %>"><%= currentState %></strong></p>
                        <p class="text-center fs-4">Time Remaining: <strong><%= timeInCurrentState %></strong> seconds</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="row mt-4">
            <% signals.forEach(signal => { %>
                <div class="col-md-3 mb-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><%= signal.name %></h5>
                            <p class="card-text">Green: <%= signal.greenDuration %> seconds</p>
                            <p class="card-text">Yellow: <%= signal.yellowDuration %> seconds</p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>